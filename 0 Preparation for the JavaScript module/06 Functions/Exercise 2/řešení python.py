# Anonymous function (lambda) that returns the largest of three numbers
largest = lambda a, b, c: max(a, b, c)

# Call the function and print the returned result
result = largest(10, 25, 15)

print(result)