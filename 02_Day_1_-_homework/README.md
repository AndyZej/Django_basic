## Exercise 1

In the directory `project` there is a project named **Django**. Inside you will find 3 applications:
* `exercises_app` &ndash; the application we worked on in class,
* `homework_app` &ndash; an app to work on at home,
* `football` &ndash; application for the additional exercise.

After class you should already have the database configured, but just to be sure, check that everything is working.
Check the `settings.py` file in the project root directory (`coderslab`) and make sure the database is configured correctly. Also check that all applications are linked to the project. Finally, you may want to take a look at the code of each application.



## Exercise 2

Create a calculator application and add it to your project.


## Exercise 3

Write a view that you will place at `/add/<value_1>/<value_2>/`.
Upon entering the page, the user should see the text:

<value_1> + <value_2> = addition result


## Exercise 4

Based on the previous exercise, add three more views that will handle the remaining mathematical operations.
